Point Generator

Generates points via task and motion planning in TMKit and then sends
the generated points across an SNS interface to a controller.


Installation instructions:

1. Install TMkit
2. Install SNS
3. autoreconf -i && ./configure && make
